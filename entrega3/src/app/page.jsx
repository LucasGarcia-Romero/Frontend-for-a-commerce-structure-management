import React from 'react';

const Home = () => {
  // Remove the extra whitespace before the opening tag of the `html` element.
  return (
    <div>
      home
    </div>
  );
};
export default Home;
